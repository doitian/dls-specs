SPECIFICATION ISpec

CONSTANTS
    Send <- MCSend
    Reply <- MCReply
    InitMemInt <- MCInitMemInt
    Proc = {p1, p2}
    Addr = {a1, a2, a3}
    Val = {v1, v2}
    NoVal = NoVal

\* PROPERTY
\* Uncomment the previous line and add property names

INVARIANT TypeInv
